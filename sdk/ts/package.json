{
  "name": "@osteosome/service-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript service SDK — stdio JSON-RPC transport + handshake + heartbeat + Service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@osteosome/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.1.1"
  }
}